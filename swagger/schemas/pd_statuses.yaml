statuses:
  title: Statuses
  description: statueses which are acceptable
  type: object
  properties:
    active:
      $ref: './pd_status.yaml#/pd_status'
    suspended:
      $ref: './pd_status.yaml#/pd_status'
    revoked:
      $ref: './pd_status.yaml#/pd_status'